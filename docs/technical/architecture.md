# Arquitectura Técnica

La arquitectura técnica se definirá de forma incremental. La intención inicial es un monorepo con frontend Next.js, backend FastAPI, PostgreSQL con pgvector, Redis/Celery y despliegue con Docker.

Ver también `ARCHITECTURE.md` en la raíz del repositorio.
